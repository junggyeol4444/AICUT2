{
  "name": "aicut-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 4173",
    "api": "python3 -m backend.server",
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'",
    "build": "vite build",
    "serve": "python3 -m backend.server",
    "check:pr": "python3 scripts/check_pr_ready.py"
  },
  "devDependencies": { "vite": "^7.1.3" }
}
